import React, {useState} from 'react';
import { Alert } from 'styled-alert-component';
import Tracker from "../Components/Cards/Tracker/Tracker";

import {CardItem} from "./ClosedCardStyle";

export default function ClosedCard({boxData}) {
    return(
        <>
        <CardItem bgColor="white"></CardItem>
        </>
    );
}